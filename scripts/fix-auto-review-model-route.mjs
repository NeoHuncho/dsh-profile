#!/usr/bin/env node
// Retired. The local dsh-deepseek-auto-review package routes its reviewer
// natively through `agentOptions.provider` and `reasoningEffort`; no installed
// package mutation is required. Keep this harmless compatibility stub so old
// manual invocations do not fail after dsh-auto-review is removed.
console.log('[fix-auto-review-model-route] retired: local DeepSeek auto-review owns its model route')
